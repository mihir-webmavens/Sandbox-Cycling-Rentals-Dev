#!/bin/bash

echo "🧹 Running Laravel Pint before commit..."
./vendor/bin/pint --test

if [ $? -ne 0 ]; then
    echo "❌ Pint found code style issues. Please fix them before committing."
    ./vendor/bin/pint
    exit 1
fi

echo "✅ Pint check passed."